---
- name: Show message on screen
  hosts: "{{ target_hosts | default('windows_hosts') }}"
  tasks:
    - name: Display message to user
      ansible.windows.win_shell: msg * "{{ user_message | default('Hello from Ansible!') }}"
